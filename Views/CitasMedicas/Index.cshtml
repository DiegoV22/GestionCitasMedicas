@model IEnumerable<GestionCitasMedicas.Models.CitaMedica>

@{
    ViewData["Title"] = "Citas Médicas";
}

<style>
    body {
        background-color: #E0F2F1; /* Color de fondo celeste suave */
    }

    .my-4 {
        text-align: right;
        margin-top: 20px;
        margin-bottom: 20px;
    }

    .table-responsive {
        margin-top: 20px;
    }

    .table-striped tbody tr:nth-of-type(odd) {
        background-color: rgba(0, 123, 255, 0.05); /* Color de fondo para filas impares */
    }

    .table-bordered {
        border: 1px solid #dee2e6; /* Borde de tabla */
        border-collapse: collapse;
    }

        .table-bordered th, .table-bordered td {
            border: 1px solid #dee2e6; /* Borde de celdas */
            padding: 0.75rem;
            vertical-align: middle;
        }

    .bg-primary {
        background-color: #007BFF; /* Color de fondo azul para el encabezado */
    }

        .bg-primary th {
            color: #fff; /* Color de texto blanco para el encabezado */
        }

    .btn-primary {
        background-color: #007BFF; /* Color de botón azul */
        border-color: #007BFF;
    }

        .btn-primary:hover {
            background-color: #0056b3; /* Color de botón azul más oscuro al pasar el cursor */
            border-color: #0056b3;
        }
</style>

<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="col-md-10">
            <h1 class="display-4">Citas Médicas</h1>

            <div class="my-4">
                <a class="btn btn-primary" asp-action="Create"><i class="bi bi-calendar-plus"></i> Programar Nueva Cita</a>
            </div>

            <div class="table-responsive">
                <table class="table table-striped table-bordered">
                    <thead class="bg-primary text-white">
                        <tr>
                            <th scope="col">Paciente</th>
                            <th scope="col">Fecha y Hora</th>
                            <th scope="col">Especialista</th>
                            <th scope="col">Motivo</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@Html.DisplayFor(modelItem => item.Paciente.Nombre)</td>
                                <td>@item.FechaHora.ToString("dd/MM/yyyy HH:mm")</td>
                                <td>@Html.DisplayFor(modelItem => item.Especialista)</td>
                                <td>@Html.DisplayFor(modelItem => item.Motivo)</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
